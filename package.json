{
  "name": "set-ui-font",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:DeepChannel/set-ui-font.git",
  "author": "Matt Walters <github@iammattwalters.com>",
  "license": "MIT",
  "devDependencies": {
    "icon-font-generator": "^2.1.10",
    "meta": "^2.2.13"
  },
  "scripts": {
    "preinstall": "meta git update",
    "generate": "rm -rf dist && mkdir dist && icon-font-generator set-ui/icons/*.svg -o dist",
    "postinstall": "yarn generate"
  }
}
